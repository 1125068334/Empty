/**
    7-1: 计算字段
        存储在表中的数据不是应用程序所需要的，需要直接从数据库中检索出转换、计算或格式化过的数据，而不是检索出数据，然后在客户端应用程序中重新格式化。
        计算字段并不实际存在于数据库表中，是运行时在SELECT语句中创建的。
        tip:
            1. 字段(field) 基本上与列(column)的意思相同，经常互换使用，不过数据库列一般称为列，而字段这个数据通常在计算字段这种场合使用
 */